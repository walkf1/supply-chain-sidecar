FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt gunicorn

COPY . .

ENV PYTHONPATH=/app
ENV PORT=8080
ENV SIDECAR_URL=https://supply-chain-sidecar-379478531186.us-central1.run.app

EXPOSE 8080

CMD ["gunicorn", "--bind", "0.0.0.0:8080", "--workers", "2", "--timeout", "60", "dashboard.app:app"]
